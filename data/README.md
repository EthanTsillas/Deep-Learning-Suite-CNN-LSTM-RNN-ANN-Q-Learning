# Data

`datasets.zip` contains the local data files used by the experiments:

```text
hrdata3.csv
news.csv
```

The code extracts the archive automatically if the raw files are not already present.

The CNN experiment uses the built-in scikit-learn digits dataset, so no image files are required.
